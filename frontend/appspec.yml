version: 0.0
os: linux

files:
  - source: /
    destination: /home/ec2-user/2023-02-CECD2-4/frontend/build
    overwrite: yes
permissions:
  - object: /
    pattern: "**"
    owner: ec2-user
    group: ec2-user
hooks:
  BeforeInstall:
    - location: ./scripts/before-install.sh
      timeout: 60
      runas: ec2-user